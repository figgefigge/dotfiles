---
- name: Install workstation packages
  ansible.builtin.dnf:
    name: "{{ packages }}"
    state: present

- name: Install fonts
  ansible.builtin.dnf:
    name: "{{ fonts }}"
    state: present

